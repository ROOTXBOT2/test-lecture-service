{
  "name": "pattern-battle-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create pattern-battle-db",
    "db:migrate": "wrangler d1 execute pattern-battle-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
